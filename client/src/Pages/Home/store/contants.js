const NS_DEVICE="device";
const SET_LOADING = `${NS_DEVICE}/setLoading`;
const GET_DEVICE = `${NS_DEVICE}/getDevice`;
const CREATE_DEVICE = `${NS_DEVICE}/createDevice`;
const SAVE_DEVICE = `${NS_DEVICE}/saveDevice`;
const UPDATE_DEVICE = `${NS_DEVICE}/updateDevice`;
const DEL_DEVICE = `${NS_DEVICE}/deleteDevice`;
const GET_DETAIL_DEVICE = `${NS_DEVICE}/getDetailDevice`;
const SAVE_DETAIL = `${NS_DEVICE}/saveDetail`;
export{
    SET_LOADING,
    GET_DEVICE,
    SAVE_DEVICE,
    UPDATE_DEVICE,
    DEL_DEVICE,
    CREATE_DEVICE,
    GET_DETAIL_DEVICE,
    SAVE_DETAIL,
}